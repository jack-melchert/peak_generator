language: python
python:
- 3.7.5
addons:
  apt:
    packages:
    - libgmp-dev
    - libmpfr-dev
    - libmpc-dev
    - verilator
install:
- pip install coreir
- pip install -r requirements.txt
- pip install pytest-cov
- pip install python-coveralls
- pip install -e .
script:
- python scripts/gen_rtl.py examples/misc_tests/test_alu.json
after_success:
- coveralls

